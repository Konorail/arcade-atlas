<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title ? `${title} · ${appName}` : appName %></title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <header class="site-header">
      <div>
        <a href="/" class="brand"><%= appName %></a>
      </div>
      <nav>
        <% if (currentUser) { %>
          <a href="/admin">后台总览</a>
          <a href="/admin/machine-types">机台类型</a>
          <a href="/admin/machines">机台管理</a>
          <a href="/admin/repairs">报修记录</a>
          <a href="/admin/auth-settings">认证设置</a>
          <a href="/logout">退出</a>
        <% } else { %>
          <a class="button small outline" href="/login">登录后台</a>
        <% } %>
      </nav>
    </header>
    <main class="page">
